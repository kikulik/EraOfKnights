# Shared Schemas

This folder contains JSON schemas and TypeScript types shared between the client and server.

- `sample_pois.json`: Example POI dataset
- `schemas/`: JSON schemas for validation
- `types/`: TypeScript types for code generation or shared models
